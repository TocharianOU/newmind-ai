# 产品简述（AttackTrace 

## 1. 产品定位
- 本产品定位为 **SOC 调查与结案助手**，从“告警出现之后”开始提供价值。  
- 核心目标是帮助分析师快速判断事件是 **真阳/误报/信息不足**，并输出可复用的证据链与报告。  
- 产品不替代 SIEM，而是把 SIEM、威胁情报、工单/CMDB、云安全数据源串成一条调查闭环。  

## 2. 产品形态（桌面 + Hub + MCP）
- 桌面 App 是唯一入口，负责交互式调查 Chat、证据呈现、报告生成与一键操作。  
- Hub 负责企业治理能力（SSO、RBAC、审批、审计、连接器管理）与团队协作。  
- MCP Host 负责工具编排与连接管理，把外部系统能力封装成可控工具。  

## 3. 数据源与工具策略（Read / Write 分层）
- 所有 MCP 工具默认以 **Read-only** 方式接入，用于取证、联查与证据补全。  
- 任何会改变外部系统状态的能力都必须归入 **Write MCP**，并在 UI 上强提示、强审计、可回滚。  
- Write 动作必须白名单化（只允许安全团队认可的“安全动作集”）。  

## 4. AWS 连接（先聚焦安全调查面）
- AWS 做成“连接器一次配置、多工具复用”，而不是每个 MCP 单独登录。  
- 优先只读覆盖：CloudTrail、Security Hub、GuardDuty、CloudWatch Logs、IAM（变更/权限线索）、VPC/EC2（资产与网络暴露面）。  
- 后续再扩展到 WAF、S3 策略、EKS/ECS/ECR（云原生客户场景）。  
- 企业长期推荐 AssumeRole（STS）拿临时凭证，个人/试用可支持本地 SSO/CLI 读取凭证。  

## 5. Elastic Cloud 连接（优先 API Key）
- Elastic Cloud/Kibana 默认使用 API Key 模式接入，降低集成摩擦并便于最小权限。  
- OIDC/SSO 跳转登录可作为企业增强能力，但不作为默认路径。  

## 6. 威胁情报与框架能力（内置差异化）
- 内置 MITRE ATT&CK 数据与映射能力，把证据链自动关联到战术/技术与缓解建议。  
- VirusTotal 等威胁情报可提供“代付托管”与“用户自带 Key（BYOK）”两种模式，企业优先 BYOK。  

## 7. 用户路径（PLG → 企业转化）
- 个人版目标是 10 分钟跑通：粘贴告警 → 一键联查 → 生成报告。  
- 企业版目标是可控可审计：SSO 登录、组织连接器、RBAC/审批、统一审计与数据策略。  
- 产品内明确区分“个人模式/组织模式”并提供可见的升级路径。  

## 8. 商业逻辑与收费锚点
- 免费/个人版卖“提效”：更快的调查与更好的报告产出。  
- 企业版卖“治理”：SSO/RBAC/审批/审计、组织连接器、合规模板与支持。  
- 计费建议按席位为主，威胁情报与模型调用按配额/用量封顶，避免成本失控。  

## 9. 最小可行版本（MVP）建议
- MVP 只做 1 条黄金链路：ES 告警 → VT/ATT&CK → 时间线/IOC → 报告导出。  
- AWS 先做只读连接器与关键服务联查，不做全量资源扫描。  
- 任何写入动作延后，并先以“生成脚本/命令供人工执行”模式落地。  

