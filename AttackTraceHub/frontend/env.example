# OAP Platform Admin Console — Environment Configuration
# Copy this file to .env and update the values
#
# In Docker: not needed (same-origin, API calls go to the Hub automatically).
# In dev mode (npm run dev): the Vite proxy forwards /api → http://localhost:23000.

# Backend API Configuration (leave empty for same-origin in production)
VITE_API_BASE_URL=
