# Root .dockerignore — used when building the Hub image (context: monorepo root)

.git
.gitignore
.vscode
.idea
.DS_Store

# Electron / Tauri — not needed for web build
electron/
dist-electron/
src-tauri/
release/
*.zip


flask_route

# Pre-existing build artifacts
dist-web/
app-dist/
oaphub/app-dist/
oaphub/frontend/node_modules/

# Node modules (installed fresh inside Docker)
node_modules/
oaphub/node_modules/

# MCP Host (has its own Docker build)
mcp-host/

# Dev / test files
temp/
prebuilt/
bin/
host-deps/
coverage/
*.log
logs/
uploads/
projects/

# Documentation
docs/

# Docker files
docker/
oaphub/docker-compose*.yml
